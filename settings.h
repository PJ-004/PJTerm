#define FONT_SIZE 12			// Font size
#define FONT "Monospace"		// Font used in the terminal
#define COLOR_SCHEME "Linux"		// Color Scheme
#define OPACITY 1.0 			// Opacity lower than 1.0 means that the window is transparent. This may not work well
